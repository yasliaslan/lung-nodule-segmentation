## Model Architecture

The proposed segmentation model (MobileNoduNet95) is implemented
using PyTorch and located in the `model/` directory.

The architecture integrates:
- A lightweight MobileNetV2 encoder
- Attention mechanisms
- Multi-scale context aggregation
- Deep supervision
